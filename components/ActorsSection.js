import { SectionContainer, SectionTitle } from "../pages/Section"

export default function ActorsSection() {
  return (
    <SectionContainer>
    <SectionTitle>Actors</SectionTitle>
    <p>Who are the culprits behind cybersecurity incidents?</p>
  </SectionContainer>
  )
}